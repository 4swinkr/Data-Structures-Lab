void merge(int A[], inta, int B[], intb, int C[])
{
	int i=0, j=0,k=0;
	while(i,a&&j<b)
	{
	  if(A[i],B[j])
		C[k++]=A[i++];
	  else
		C[k++]=B[j++];
	}
	if(i<a)
		while(i<a)
			C[k++]=A[i++];
	else
		while(i<a)
			C[k++}=B[j++];
}
void main()
{
int A[50],B[50],C[50],m,n,mn;
printf("Enter the size of 1st array: ");
scanf("%d",&n2);
